<%= form_for(:survivor) do |f| %>

	<%= f.select :name,
	  Survivor.all.map { |f| [f.name, f.id] },
	               { include_blank: true },
	               { class: 'chosen-select'}
	%>

<% end %>

<h2>Items</h2>

<%= form_for(:trade) do |f| %>

	<%= f.select :name,
	  Trade.all.map { |f| [f.name, f.id] },
	               { include_blank: true },
	               { class: 'chosen-select'}
	%>

<% end %>

<br>

<h2>second Survivor</h2>

<%= form_for(:survivor) do |f| %>

	<%= f.select :name,
	  Survivor.all.map { |f| [f.name, f.id] },
	               { include_blank: true },
	               { class: 'chosen-select'}
	%>

<% end %>
<br>

<h2>Items</h2>

<%= form_for(:items) do |f| %>

	<%= f.select :name,
	  Item.all.map { |f| [f.name, f.id] },
	               { include_blank: true },
	               { class: 'chosen-select'}
	%>

<% end %>